/*
    FOR EACH PROBLEM BELOW, IMPLEMENT THREE WAYS
    1. AS A FUNCTION DECLARATION
    2. AS AN ANONYMOUS FUNCTION EXPRESSION
    3. AS A NAMED FUNCTION EXPRESSION
    
    RUN BOTH TO ENSURE THEY WORK PROPERLY
*/


/*
    PROBLEM 1:
        Write a function that takes THREE params: a, b, c
        return the remainder of the sum of a,b,c divided by 3

    SOLUTION: */ 
        //FUNCTION DECLARATION:
        // function someFunction (a, b, c){
        //     return (a + b + c)% 3;
        // }  
        
        // console.log(someFunction(1,2,3));

        // //ANONYMOUS FUNCTION EXPRESSION
        // const someFunction2 = (a,b, c) => {
        //     return (a + b + c) % 3;
        // }

        // console.log(someFunction2(1,24,5));

        //  //NAMED FUNCTION EXPRESSION
        //  const someFunction3 = function (a,b, c){
        //     return (a + b + c) % 3;
        // }

        // console.log(someFunction3(1,24,5));




/*
    PROBLEM 2:
        Write a function that takes THREE params: a,b,c
        return a string that addes a dash in between them.
        
        so, if a = 'one', b = 'two', c='three'
        then function will return 'one-two-three'
    SOLUTION*/
         // FUNCTION DECLARATION:
        // function someFunction (a, b, c){
        //     return `${a}-${b}-${c}`
        // }  
        
        // console.log(someFunction('one','two','three'));

        // //ANONYMOUS FUNCTION EXPRESSION
        // const someFunction2 = (a,b, c) => {
        //     return `${a}-${b}-${c}`
        // }

        // console.log(someFunction2('one','two','three'));

        //  //NAMED FUNCTION EXPRESSION
        //  const someFunction3 = function (a,b, c){
        //     return `${a}-${b}-${c}`
        // }

        // console.log(someFunction3('one','two','three'));


/*
    PROBLEM 3:
        Write a function that takes NO params
        return a random number between 0 and 10 
        (doesn't have to be a whole number tho)

    SOLUTION:*/

     // FUNCTION DECLARATION:
    //  function someFunction (){
    //     return Math.random();
    // }  
    
    // console.log(someFunction());

    // //ANONYMOUS FUNCTION EXPRESSION
    // const someFunction2 = () => {
    //     return Math.random();
    // }

    // console.log(someFunction2());

    //  //NAMED FUNCTION EXPRESSION
    //  const someFunction3 = function (){
    //     return Math.random();
    // }

    // console.log(someFunction3());
